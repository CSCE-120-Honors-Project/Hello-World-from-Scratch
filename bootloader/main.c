// Minimal bootloader C entry point used for testing the build system.
// In the real project this would load the OS from FAT and jump to it.
void main(void) {
    // For build/testing purposes, just loop here.
    while (1) { }
}
